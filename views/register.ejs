<!DOCTYPE html>
<html lang="en">
    <%- include('layouts/header') -%>

    <body>
        <!-- Navigation-->
        <%- include('layouts/navbar') -%>

        <!-- Page Header-->
        <header class="masthead" style="background-image: url('/img/post-sample-image.jpg')">
            <div class="container position-relative px-4 px-lg-5">
                <div class="row gx-4 gx-lg-5 justify-content-center">
                    <div class="col-md-10 col-lg-8 col-xl-7">
                        <div class="page-heading">
                            <h1>Register New Account</h1>
                            <span class="subheading">Wanna post blogs? Please register first.</span>
                        </div>
                    </div>
                </div>
            </div>
        </header>
        
        <!-- Main Content-->
        <main class="mb-4">
            <div class="container px-4 px-lg-5">
                <div class="row gx-4 gx-lg-5 justify-content-center">
                    <div class="col-md-10 col-lg-8 col-xl-7">
                        <p>Want to get to start blogging? Fill out the form below to register!</p>
                        <div class="my-5">
                            <% if(errors != null && errors.length > 0){ %>
                                <ul class="list-group">
                                <% for(var i = 0; i < errors.length; i++){ %>
                                    <li class="list-group-item list-group-item-danger"><%= errors[i] %></li>
                                <% } %>
                                </ul>
                            <% } %>
                            <form method="post" action="/users/register" enctype="multipart/form-data">
                                <div class="form-floating">
                                    <input type="text" name="username" class="form-control" value="<%= username %>"/>
                                    <label for="username">Username</label>
                                </div>

                                <div class="form-floating">
                                    <input type="password" name="password" class="form-control" value="<%= password %>" />
                                    <label for="password">Password</label>
                                </div>
                                <br/>
                                <div class="form-floating">
                                    <button type="submit" class="btn btn-primary text-uppercase enabled">Register</button>
                                </div>
                            </form>
                        </div>
                    </div>
                </div>
            </div>
        </main>

        <!-- Footer-->
        <%- include('layouts/footer') -%>

        <!-- Scripts -->
        <%- include('layouts/scripts') -%>
   
    </body>
</html>
